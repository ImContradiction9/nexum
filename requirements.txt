fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
# Janela nativa do app (Edge WebView2, embutido no Windows) — sem depender do
# Chrome. pythonnet vem junto (backend .NET do WebView2). Se faltar, cai pro navegador.
pywebview>=5
# Leitura de PDF de faturas (puro pip, binário embutido — substitui o Poppler).
pypdfium2>=4
